import React, { Component } from 'react';

class Footer extends Component {
  render() {
    return (
     <footer>
     Psn:G-Wackx, forever chasing 40 kills in a single game.
     </footer>
    );
  }
}

export default Footer;